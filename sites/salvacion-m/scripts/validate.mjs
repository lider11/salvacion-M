import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const src=await readFile('dist/server/index.js','utf8');const mod=await import(`data:text/javascript;base64,${Buffer.from(src).toString('base64')}`);assert.equal(typeof mod.default?.fetch,'function');const manifest=JSON.parse(await readFile('dist/.openai/hosting.json','utf8'));assert.equal(manifest.d1,'DB');console.log('Worker, manifest and D1 binding valid');
